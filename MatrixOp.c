#include "MatrixOp.h"
int* Add(int A[SIZE][SIZE], int B[SIZE][SIZE])
{
    int sum[SIZE][SIZE];
    for(int i=0;i<SIZE;i++)
    {
        for(int j=0;j<SIZE;j++)
        {
            sum[i][j]=A[i][j];
        }
    }
}